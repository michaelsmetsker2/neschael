;
; neschael
; lib/game/entities/entityData.inc
;
; constants and macros relating to the entity management system
;

    ; amount of entity slots in the pool
  ENTITY_POOL_SIZE = $0F 
    ; how many bytes of ram per entity
  ENTITY_LENGTH    = $07
